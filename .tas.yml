---
framework: jest
preRun:
     command:
        - yarn install
preMerge:
  pattern:
    - "./test/stories.ts"
postMerge:
  pattern: 
    - "./test/stories.ts"
postRun:
     command:
        - yarn --version
version: 1.0